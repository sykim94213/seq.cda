% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cda_glm_fxfx.R
\name{cda_glm_fxfx}
\alias{cda_glm_fxfx}
\title{Causal decomposition under fixed intervention targets}
\usage{
cda_glm_fxfx(
  med_covariates = NULL,
  base_covariates,
  group,
  confounders1,
  confounders2,
  mediator1,
  mediator2,
  outcome,
  data,
  comparison,
  reference,
  mediator1_fixed,
  mediator2_fixed,
  cluster_id,
  n_bootstrap = 1000,
  seed = 123,
  verbose = TRUE
)
}
\arguments{
\item{med_covariates}{Optional character vector of post-treatment mediator
covariates to include in the outcome model. Use \code{NULL} if no mediator
covariates are included.}

\item{base_covariates}{Character vector of baseline covariate names measured
before the first intervention target.}

\item{group}{Character string giving the name of the group variable in
\code{data}.}

\item{confounders1}{Character vector of confounder names measured before the
first intervention target.}

\item{confounders2}{Character vector of confounder names measured after the
first intervention target but before the second intervention target.}

\item{mediator1}{Character string giving the name of the first intervention
target in \code{data}.}

\item{mediator2}{Character string giving the name of the second intervention
target in \code{data}.}

\item{outcome}{Character string giving the name of the outcome variable in
\code{data}.}

\item{data}{A data frame containing the variables used in the analysis.}

\item{comparison}{Character string giving the comparison group level of
\code{group}.}

\item{reference}{Character string giving the reference group level of
\code{group}.}

\item{mediator1_fixed}{Value to which the first intervention target is fixed
under the hypothetical policy.}

\item{mediator2_fixed}{Value to which the second intervention target is fixed
under the hypothetical policy.}

\item{cluster_id}{Character string giving the cluster identifier used for the
clustered bootstrap.}

\item{n_bootstrap}{Integer giving the number of bootstrap resamples used to
compute standard errors. Default is \code{1000}.}

\item{seed}{Integer random seed for reproducibility. Default is \code{123}.}

\item{verbose}{Logical; if \code{TRUE}, prints progress messages during estimation.}
}
\value{
An object of class \code{"cda_glm_fxfx"} containing:
\describe{
\item{tau_glm}{Estimated initial disparity, computed as the bootstrap mean
of the total disparity across replicates.}
\item{delta_glm}{Estimated disparity reduction under the policy that fixes
both intervention targets to \code{mediator1_fixed} and \code{mediator2_fixed},
computed as the bootstrap mean across replicates.}
\item{zeta_glm}{Estimated disparity remaining under the fixed-target policy,
computed as the bootstrap mean across replicates.}
\item{tau_glm_se}{Bootstrap standard error for \code{tau_glm}.}
\item{delta_glm_se}{Bootstrap standard error for \code{delta_glm}.}
\item{zeta_glm_se}{Bootstrap standard error for \code{zeta_glm}.}
\item{meta}{A list of metadata including the number of bootstrap
replicates, random seed, group variable name, comparison group,
reference group, and sample size.}
}
}
\description{
This function implements a sequential causal decomposition analysis for two
time-ordered intervention targets. It estimates the initial disparity, the
disparity remaining, and the disparity reduction under a hypothetical policy
that fixes both targets to specified values for all individuals, regardless
of group membership.
}
\examples{

\dontrun{
cda_glm_fxfx(med_covariates = NULL, base_covariates = c("C1", "C2"),
  group = "group", confounders1 = c("L1", "L2"), confounders2 = c("L3", "L4"),
  mediator1 = "M1", mediator2 = "M2", outcome = "Y", data = dat,
  comparison = "Group1", reference = "Group0",
  mediator1_fixed = 1, mediator2_fixed = 1,
  cluster_id = "cluster", n_bootstrap = 500, seed = 123, verbose = TRUE)
}
}
\references{
Park, S., Kim, S. Y., Zheng, X., & Lee, C. (2025).
Causal decomposition analysis with synergistic interventions:
A triply robust machine-learning approach to addressing multiple dimensions
of social disparities. Psychological Methods. Advance online publication.
https://doi.org/10.1037/met0000803
}
\author{
Soojin Park, University of California, Riverside, \email{soojinp@ucr.edu}.
}
